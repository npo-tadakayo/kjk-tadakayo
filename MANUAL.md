# タダカヨの介護情報基盤伴走支援 — 操作マニュアル

> 対象読者: タダカヨ事務局スタッフ / 認定事業所の発注ご担当者
> 管理画面: https://kjk-tadakayo-admin.web.app
> 認定事業所ポータル: https://kjk-tadakayo-admin.web.app/partner.html
> 最終更新: 2026-06-07

このマニュアルは、専門知識がなくても操作できるよう手順中心で書いています。システムの技術仕様（業務フロー図・データ構造など）は `ENGINEERING_NOTES.md` を参照してください。

---

## §1 このシステムは何か

介護情報基盤の伴走支援に関する一連の業務を、1つの管理画面でまとめて扱うためのシステムです。

- 問い合わせの受付から、見積もり・案件管理・伴走支援・助成金申請、カードリーダーの発注／在庫／出荷／請求までを、流れを通して管理できます。
- LP（kjk.tadakayo.jp）と見積もりツールからの問い合わせは、自動で「案件」として取り込まれ、Google Chat にも通知されます。

---

## §2 できること

| 機能 | 内容 |
|---|---|
| ダッシュボード | 件数・想定助成金額・進捗の全体像 |
| 案件一覧 | すべての案件。検索・絞り込み・CSV出力 |
| カンバン | ステータス別のカードで管理（ドラッグで移動） |
| 案件詳細 | 対応履歴・伴走支援（写真）・申請情報の管理 |
| AIアシスタント | 返信下書き・要約・伴走報告文の作成 |
| メール送信 | 案件から直接メール送信（タダカヨ事務局名義） |
| 報告書PDF | 支援報告書の出力 |
| 供給管理 | 在庫・発注・出荷・受注・請求・入金 |
| 認定事業所ポータル | 認定事業所が Web からカードリーダーを発注 |
| 自動フォロー | 毎朝9時に停滞案件などを Chat 通知 |
| ユーザー管理 | ログインできる人と権限の管理 |
| 設定 | 通知先・メール送信元・振込先・印影などの登録 |

---

## §3 始める前に

### ログイン
1. 管理画面 https://kjk-tadakayo-admin.web.app を開く
2. 「Google でログイン」→ @tadakayo.jp のアカウントを選ぶ

> [!WARN]
> ログインできるのは「ユーザー管理」に登録された @tadakayo.jp のアカウントだけです。新しいスタッフは、管理者が「ユーザー管理」から追加してください。

### 権限（ロール）
| ロール | できること |
|---|---|
| 管理者（admin） | すべての操作＋設定・ユーザー管理 |
| 担当者（staff） | 日常の案件・供給業務 |
| 閲覧（viewer） | 閲覧中心 |

### 認定事業所の方
認定事業所ポータル（partner.html）に Google アカウントでログインします。事前にタダカヨ事務局での許可リスト登録が必要です。スマートフォンからもご利用いただけます。

---

## §4 使い方

### 案件を進める
1. 案件一覧またはカンバンから案件を開く
2. ステータスを現在の状況に変更（履歴に自動で記録されます）
3. 「対応記録」に電話・メール・訪問などを記録
4. 「伴走支援」で訪問日・メモ・写真を残す
5. 「申請情報」で助成金の申請状況を管理

### AIアシスタントを使う
案件詳細の「AIアシスタント」タブで、次のことができます。
- 問い合わせ内容から返信メールの下書きを作る
- 案件のポイントを要約・分類する
- 伴走メモから報告文を作る
- 下書きは「送信欄へ転記」→ そのままメール送信できます

> [!WARN]
> AI が作った文章は、必ず内容を確認・修正してから送ってください。AI はあくまで下書きの補助です。

### 報告書PDFを出す
1. 案件詳細の右上「報告書PDF」をクリック
2. 開いたページで「印刷 / PDF保存」

### カードリーダーの発注・入荷・出荷（供給管理）
- **発注（下書き → 確定して送付）**:
  1. 「新規発注」で商品の数量・希望納期を入力 →「下書きとして保存」
  2. 一覧の下書きは「編集」で修正、「削除」で取消ができます
  3. 「確定」を押すと、宛先（ABサークル）・件名・本文・**発注書PDFのプレビュー**が開きます（その場で編集可）
  4. 「この内容で送信」で、発注書PDFを添付してABサークルへメール送信されます（発注済になります）
  5. 商品が届いたら「入荷登録」（在庫が増えます）
  - 発注先メール・CC・メール定型文は「設定」で登録します。数量に応じて卸単価（1台 / 2-10台 / 11-30台 / 31台〜）が自動で切り替わり、**お届け地域を選ぶと送料も自動入力**されます
  > [!WARN]
  > 「この内容で送信」は実際にABサークルへメールが届きます。初めて試すときは、宛先（TO）を自分のアドレスに変えて送信し、PDFの見た目を確認してから本番の宛先で送ってください。
- **認定事業所へ直送する場合（発注から出荷を自動作成）**: 発注モーダルで「**認定事業所へ直送する**」にチェックし、**請求先の認定事業所**を選びます（届け先住所は自動入力。エンド事業所へ送る大ロット時は手入力で上書き）。「確定して送付」すると、ABサークルへの発注と同時に**出荷の「下書き」が自動で作られます**（タダカヨ倉庫を経由しないため在庫は動きません）。商品が届いたら、出荷タブの「下書き」を「**出荷を確定**」→ 請求書・送付状を発行できます。単価は認定事業所の卸価格で自動計算されます。
- **出荷（直接・自社から発送）**: 「新規出荷」で送付先と数量を入力（在庫が自動で減ります）。配送方法で「レターパック」「ゆうパック」を選ぶと送料が自動入力され、送付状・宛名・請求書PDFを発行できます
  - **送料は税抜で入力**します（発注の送料欄と同じ基準）。配送方法を選んだときは、料金表の税込実費から税抜に換算して自動で入ります。請求書では税抜のまま明細に載り、消費税は最後にまとめて計算されます
- **レターパック宛名**: 出荷やパートナー管理から、赤（プラス）／青（ライト）の宛名を印刷できます（差出人を選べます）

### 受注 → 請求 → 入金
- **直接支援する事業所**: 出荷（定価）→ 請求書 → 入金を記録
- **認定事業所**: ポータルからの受注 → 「出荷へ」（卸価格・直送）→ 請求書 → 入金を記録
- 出荷タブの上部バッジで「未請求（発送済）／未集金・残額／うち支払期限超過／入金済・累計」を確認できます
- 請求書は適格請求書（インボイス）に対応しています。振込先は設定画面で登録してください

#### 入金の記録（一部入金にも対応）
1. 請求済の出荷にある「入金記録」を押します
2. 請求額・入金済・残額・支払期限が表示されます。金額（既定は残額）と入金日を入れて「この入金を記録」
3. **残額が0になると「入金済」になり、領収書が出せるようになります。** 残る場合は「請求済」のまま一部入金として記録され、一覧に「未集金 ¥○○（一部入金 ¥△△）」と出ます
- 振込手数料が引かれて入ってきたときも、そのままの金額で記録すれば残額として残ります（備考欄に「振込手数料差引」と書いておくと後で分かります）

#### 多く入金された（過入金）とき — 次回の請求から引く
- 請求額より多い金額を記録すると、確認のうえ **過入金** として残ります（返金処理はしません）
- 過入金がある請求先は、出荷タブの上部に「**過入金・未充当（次回請求に充当）**」の金額が出て、未集金の表にも「過入金（充当できる分）」「差引後の請求」の列が出ます
- **次の請求で「請求済にする」を押すと、「過入金 ¥○○ を充当しますか？」と確認が出ます。** はいを選ぶと請求額から差し引かれ、残額が減ります（あとから充当したい場合は、請求済の行の「過入金を充当」ボタンでも同じことができます）
- 充当すると **請求書に「前回お預かり分の充当 −¥○○」と「今回お支払額」の行が入る**ので、相手には差引後の金額を振り込んでもらえます
- 過入金が次の請求額より多い場合は、引ききれなかった分がそのまま残高として残り、さらに次の請求で使えます
- 古い過入金から順に使います（複数ある場合）。どの出荷の過入金を充当したかは請求書と記録に残ります
- **二重に入金された場合**（同じ請求に満額が2回振り込まれた等）も同じ扱いです。次回請求からの差し引き、または下記の返金のどちらかを選べます

#### 返金したとき
- 入金記録の画面に「**返金を記録する**」があります。金額（既定は未充当の過入金の額）・返金日・返金方法（振込／現金／相殺）・備考を入れて記録します
- 履歴は入金と返金が**日付順に並び、純入金の累計が右に出ます**（通帳のように追えます）。誤りは×で取り消せます
- 過入金を返金すると充当できる残高は0になります（返金した分は次回請求から引かれません）
- **返品・キャンセルで請求額まで返金した場合**は、入金が請求額を下回るため状態が「入金済」から「請求済（未集金）」に戻ります。取引そのものを取り消す場合は、出荷を削除してください（在庫も戻ります）
- 上部バッジの「入金済・累計」は返金を差し引いた純額です

#### 入金が複数回にわたるとき
- **入金は何回でも記録できます。** 2回・3回に分かれた振込も、そのつど「入金記録」で追加すれば累計され、残額が0になった時点で「入金済」になります
- 一覧には「入金2回」と回数が出るので、分割で入っていることがひと目で分かります
- 入金済のものは「入金履歴」から履歴を確認できます。誤って記録したときは履歴の×で取り消せます（取り消すと自動で「請求済」に戻ります）

#### 未集金の確認と催促
- 出荷タブの上に **「未集金（請求先ごと）」** の表が出ます。請求先ごとの残額・対象の出荷番号・支払期限が並び、**超過があるものが上に来ます**
- **支払期限は「請求月の翌月末」**（請求書の記載と同じ）で自動計算されます。超過すると一覧に「○日超過」と赤で出ます
- 期限を過ぎた出荷には **「催促メール」** ボタンが出ます。宛先・件名・本文が入った状態で開くので、必要なら文面を直して送信してください（送信元は発注書メールと同じ事務局アドレス）。送った日は記録され、ボタンに「7/30送信済」と表示されます。何度でも送れます
- 文面は「行き違いでお振込みいただいていたらご放念ください」と添える書き方にしています（相手を責めない文面）
- **領収書**: 入金済みの出荷には「領収書」ボタンが表示されます。明細は画面上でそのまま編集・行追加でき（例: 伴走支援サポート費を追記）、各行に助成金の用途区分（A=カードリーダー／B=接続サポート等経費／対象外=送料等）を付けると、区分ごとの税込小計が自動計算されます（助成金申請額との突合に使えます）。但し書きは上部の入力欄で変更できます。合計5万円以上のときは収入印紙欄が出ます（紙で発行する場合のみ貼付。PDF送付なら不要）。印影は請求書と同じタダカヨの角印が入ります
- **領収書の発行記録**: 領収書画面の「発行を記録」を押すと、領収書番号・発行日・金額・但し書き・明細・用途区分の集計が保存されます。出荷一覧のボタンが「領収書（発行済 7/30）」になり、**誰にいつ何を発行したか後から確認できます**。同じ内容で再発行したいときは、その画面をもう一度開けば保存した内容で出せます（内容を直したら「記録を更新」を押してください）

### 認定事業所からの発注を受ける

**方法1: ポータルから受け取る（おすすめ）**
1. 供給管理「パートナー管理」で、認定事業所のログイン用メールと名称を登録
2. 認定事業所に partner.html のURLを案内
3. 届いた発注は、供給管理「受注（認定事業所）」で確認・ステータスを更新

**方法2: 発注ファイル（CSV／JSON）をメールで受け取る**

ポータルを使わない相手には、`発注テンプレート.csv` または `発注スキーマサンプル.json` をお渡しし、記入したファイルをメールで送ってもらいます。

1. 供給管理 →「受注（認定事業所）」タブ →「**発注ファイルを取り込む**」を押す
2. 届いたファイルを選ぶ（**複数まとめて選べます**。CSVとJSONの混在も可）
3. 読み取った内容がプレビュー表に出るので確認する
   - 品番の誤り・数量0・住所の抜けなどがあると、**何行目の何が問題か**が赤く表示されます。直してもらってから取り込んでください
   - すでに取り込んだ発注番号は「取り込み済み」と出て、二重登録されません
4. 「受注として登録」を押すと、**受注番号（SO-2026-0001 など）が自動で付いて**一覧に並びます

- 1つの発注で複数の品番を頼まれた場合、CSVでは同じ `partner_order_no` で行を分けてもらいます（自動で1件の受注にまとまります）
- 取り込んだあとの流れはポータル経由と同じで、「出荷へ」→ 請求書 → 入金です

### 設定（管理者のみ）
次の項目を登録・変更できます。変更は保存後、最大60秒で反映されます。
- 通知先（Google Chat Webhook URL）／ メール送信元
- 請求書の振込先口座
- 発注書の発行元・代表者・印影
- 仕入先（ABサークル）の送付先メール・CC・担当者、発注メールの定型文（件名・本文）
- 適格請求書の登録番号（T＋13桁）
- レターパックの差出人

### ユーザー管理（管理者のみ）
ログインできる人の追加・編集・停止、権限の変更ができます。自分自身を誤って締め出さないよう、自分の削除・停止・権限降格には確認が入ります。

---

## §5 よくある質問

### Q. 問い合わせはどうやって入ってきますか？
LP・見積もりツールから送信されると、自動で案件として登録され、Google Chat にも通知されます。

### Q. メールはどのアドレスから送られますか？
「タダカヨ事務局 <kjk-staff@tadakayo.jp>」から送られます。設定画面で変更できます。

### Q. 通知先の Chat を変えたい
設定画面の「Google Chat Webhook URL」を変更して保存してください（最大60秒で反映）。

### Q. スマートフォンでも使えますか？
使えます。画面が狭いときは、左上のメニューボタン（≡）からメニューを開けます。認定事業所ポータルも同様です。

### Q. AIの文章はそのまま送って大丈夫？
必ず内容を確認・修正してから送ってください。AI は下書きの補助です。

---

## §6 困ったとき

> [!WARN]
> **ログインできない**: @tadakayo.jp のアカウントでログインしているか、「ユーザー管理」に登録されているかを確認してください。認定事業所の方は、事務局での許可リスト登録が必要です。

> [!TIP]
> **メールが送れない**: 設定画面の「メール送信元」が正しいアカウントかを確認してください。解決しない場合は事務局へご連絡ください。

---

## §7 連携している仕組み

| 連携先 | 内容 |
|---|---|
| LP（kjk.tadakayo.jp） | 問い合わせを自動で案件化 |
| 見積もりツール（/mitsumori.html） | 成約を自動で案件化 |
| Google Chat | 受信・成約・自動フォローの通知 |
| Gmail | 案件からのメール送信（タダカヨ事務局名義） |
| AI（Vertex AI / Gemini） | 返信下書き・要約・報告文の生成 |

---

## §8 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-06-05 | MANUAL.md を新規作成（アプリ内マニュアル manual.html を正本＝SSOT化） |
| 2026-06-05 | 発注を「下書き→確定して送付」に。確定で発注書PDFを添付してABサークルへメール送信（プレビュー編集可）。設定に仕入先・発注メール定型文を追加。発注単価を数量帯別に |
| 2026-07-28 | **認定事業所からの発注ファイル（CSV／JSON）を取り込めるように**（受注タブ「発注ファイルを取り込む」）。複数ファイルまとめて可・誤りは行番号つきで表示・二重取込の防止・受注番号（SO-）の自動採番。あわせて**出荷の送料を税抜入力に統一**（発注の送料欄と同じ基準に。配送方法を選んだときは料金表の税込実費から自動換算） |
| 2026-06-07 | 出荷送料を配送方法（レターパック/ゆうパック）から自動計算し請求書に計上／ダッシュボードに月次推移グラフ／申請期限を設定画面から変更可能に／**認定事業所への直送発注で出荷の下書きを自動生成（2段階）**／補助金区分の訂正（居宅療養管理指導・地域密着型通所介護→訪問・通所系¥64,000）／スマホ・文字の見やすさ（アクセシビリティ）改善 |
| 2026-07-02 | **入金済み出荷の領収書発行を追加**（編集可能な明細・助成金の用途区分A/Bごとの税込小計・但し書き編集・5万円以上で収入印紙欄・タダカヨ角印）／帳票・支援報告書のデザインをタダカヨ赤に統一（旧・緑の残りを解消） |
