# タダカヨの介護情報基盤伴走支援 LP / CRM 申し送り — 2026-09-18（Issue #5 の3機能・マニュアル改訂・動画8本・チラシ差し替え・伴走支援費付き出荷 まで本番反映済み。09-16 チラシ再差し替え／09-18 合計見積もりの案内資料）

> 📁 **2026-09-13 に、9/11 までの完了済みの作業記録を `HANDOFF_ARCHIVE.md` へ移した**（承諾書・㉔セッション・メール送信元・Formspree・見積もり改修の一次/二次・申込モーダルの不具合）。
> 経緯を追うときはそちらを見る。

## 2026-09-16〜18 の対応（チラシ再差し替え・赤ボタン・合計見積もりの案内資料）

| # | 何 | 状態 |
|---|---|---|
| 出荷一覧の赤ボタン（09-14 夜） | 請求書メールを送っても状態は発送済のまま（`invoiceMailedAt` のみ記録）。発送済の行の「請求済にする」を赤（次の一手）に | ✅ admin live `73b1925418babcc3`・commit `0dfc987` |
| チラシ再差し替え（**09-16 朝**） | 次田さんから新版（同名末尾スペース付き・08:00 版・md5 `16ede467…`）。`images/tadakayo_kjk_flyer_20260916.pdf/.jpg` に版付け直し、`_202609` は `images/_旧版/` へ。index.html の参照3か所を置換。サムネイルは `pdftoppm -r 110`（910×1287） | ✅ lp live `723fafe2f8f9c496`・commit `9fbbf41`（push 済） |
| **合計お見積もりのご案内（09-18）** | 次田さん依頼「見積書の前に付ける、法人内で決裁に出せる横長の資料（申込→助成金申請の流れ入り）」。`scripts/quote-cover/build.mjs 入力.json 出力.html` → `CHROME_EXE=… python3 scripts/quote-cover/topdf.py 出力.html 出力.pdf`（A4横・8p）。金額は `functions/estimate-pricing.js` で計算（手計算なし）。入力例 `sample.json`。サンプルPDF: `_アウトボックス/20260918_介護情報基盤_合計見積もりのご案内_サンプル/`。⚠ Python Playwright は 1223 のブラウザを要求するが未導入 → `CHROME_EXE` で `chromium_headless_shell-1243` を指す。フォントは Google Fonts をローカル描画時のみ読み込む（配布物には埋め込まれた文字だけ） | ✅ 手動生成。CRMからの自動生成は未着手（次田さんの確認後に検討） ／ ⚠ **表紙に角印は押さない**（2026-09-20 次田さん指示・`6f4a79d` で一度押したが `736c3d2` で撤去）。「角印はどこにありますか？」は**データの所在の確認**であって押印の指示ではなかった |

## 現在の状態（2026-09-14 午後・Issue #5 実装後）

- **Issue #5 の未着手3機能を実装**（下の節）。**Functions 6本は本番反映済み**（10:35 / 11:0x JST・後方互換）。**Hosting（lp・admin）も live 昇格済み**（lp live `6a453812c0183508`（CSS修正後）／admin live `7fe79a7484138e2e`・11:1x JST・次田さん承認）。
  - lp preview: https://kjk-tadakayo--sess-edit-aqvlhy8r.web.app（version `3805d44cd529cbb8`）
  - admin preview: https://kjk-tadakayo-admin--sess-edit-1tasqzda.web.app（version `e600f45a4b8e1c9a`）
- コミット: 本節の実装は 1コミット（`feat(見積もり): Issue #5 …`）で push 済み（ハッシュは git log）
- 通し確認は本番 Functions ＋ プレビュー Hosting で実施し、**テストデータ（案件 #157〜159・EST-2026-0023〜0025・SH-2026-0019/0020・事業所3・記録・leadToken・Storage PDF）はすべて削除済み**（0件を確認）。番号は進んでいる。認可ドメインは本番4件に戻した
- Chat 通知（🛒 お申し込み 2事業所／📥 LP問い合わせ ご希望: 正式に申込みたい）が基盤スペースに流れているはず。**テスト投稿なので無視してよい旨を一言入れると親切**

## 夕方の追加対応（2026-09-14 18:00〜19:20・次田さん指示）

| # | 何 | 状態 |
|---|---|---|
| チラシ | LPの紹介チラシを **2026年9月版（A4 表裏2ページ・`2605_NPO法人タダカヨ_介護情報基盤伴走支援事業.pdf`）** に差し替え。⚠ 画像・PDFは `cache-control: immutable（1年）` のため**同名で上書きすると再訪者に旧版が出続ける** → `images/tadakayo_kjk_flyer_202609.pdf/.jpg` と版付きに改名。旧版は `images/_旧版/`（hosting ignore） | ✅ lp live `1ee1bcf0bfe7a7d4`・commit `a2861f7` `9b3f6a7` |
| 報告書2本 | `docs/改修とテスト結果_20260914.md`（→ `00_outbox/_リリース記録/20260914_介護情報基盤_改修とテスト結果.pdf`・release-index で配置・索引6件）／`docs/PMS報告_見積もり機能の追加と個人情報の扱い_20260914.md`（→ `00_outbox/介護情報基盤_資料一式_20260902/15_…pdf`）。md→HTML（python-markdown）→ Playwright `page.pdf`（scratchpad `html2pdf.mjs`） | ✅ commit `a41ae46` |
| **伴走支援費を含む請求書（仕組みの穴）** | 次田さん発見: 伴走支援費入りの請求書は **Web申込で自動作成された出荷だけ**（`acceptQuote` が support-fee／discount 行を入れる）。CRM「新規出荷」で手作りした出荷は商品だけ → 請求書がカードリーダーのみ（ケアプラン愛 SH-2026-0015 ¥43,500）。さらに「送付先・数量を直す」で Web申込出荷の2行が**消える**穴もあった | ✅ 修正・admin live `5a3952ca47c0421e`・commit `caeba36` |
| ↳ 実装 | 出荷モーダル（新規・修正／事業所向けのみ）に**「伴走支援費を含める」**: プラン（訪問・通所／居住・入所／その他）＋補助対象台数（BT/USB・商品数量から自動）→ `EstimatePricing.computeAmounts` で `伴走支援費（補助対象N台）`・`金額調整` の2行を生成（acceptQuote と同じ形）。修正時は既存行から復元、プラン未保存なら紐づく見積もり→案件から補う（`inferSubsidyFromLinks`）。請求済以降はロック。`admin/js/estimate-pricing.js` を追加（3ファイル同期を `check-pricing-sync.sh` で確認）。acceptQuote も出荷下書きに `subsidyCategory/subsidyQty` を保存するように（Functions 反映） | ✅ |
| ↳ 実データ | **SH-2026-0015（ケアプラン愛）に訪問・通所 BT3 で伴走支援費を付けた → 請求書 ¥64,000**（伴走支援費 ¥50,000・金額調整 −¥29,500）。在庫は動かない（nonStock 行） | ✅ 次田さんが請求書を発行する |
| ↳ 検証 | サンプル出荷2件（手作り／Web申込形式）を本番に直接作って UI で保存・復元を確認 → 削除済み。計算3パターンは Node で一致確認 | ✅ |

## マニュアル改訂・動画8本の作り直し（2026-09-14 午後・進行中）

**次田さん指示「マニュアルと動画の改訂に移ってほしい」**。Fable 司令塔＋Sonnet 4体（マニュアル 27万／台本 23万／スライド文言 23万／写真差し替え 実行中）。撮影は司令塔が直営。

| # | 何 | 状態 |
|---|---|---|
| マニュアル | `MANUAL.md`（+122行・§4 に見積もり3機能・9/12〜14 分の節・§8 変更履歴）／`admin/manual.html`（同内容）／新規 `docs/CRM改訂のお知らせ_20260914.md`（旧名称→新名称の対応表つき） | ✅ commit `32748b1`・admin live `ab56ea324ae4bb4f` |
| 配布PDF | `00_outbox/介護情報基盤_資料一式_20260902/10_介護情報基盤CRM_操作マニュアル_20260914.pdf`（15p）・`14_2026-09-14_CRM改訂のお知らせ_v1.0.pdf`（3p）。旧 10_ は `_旧版/` | ✅ 配置済み。PDF は文言修正後に出し直し済み（14p・13:23）。作り方: `admin/` を `python3 -m http.server 8788` で配信し、Playwright で `docpage.js`/`sidebar.js` 等を空応答に差し替えて `page.pdf`（スクリプトは scratchpad の `manual_pdf.mjs`＝再利用するならリポの scripts/ へ） |
| 角印・ボタン幅（次田さん指摘） | 見積書2種（Web `mitsumori.html`・CRM `quote-doc.js`）の角印を右上ヘッダー→**発行者（法人名）欄の横**へ（請求書・領収証は元からこの置き方）／ホーム「はじめての方へ」右3ボタンを `min-width:200px` で統一 | ✅ lp live `3bdb6f138623caf1`・admin live `ab56ea324ae4bb4f` |
| 台本 U1〜U8 | 旧名称→新名称（供給管理／ダッシュボード／出荷を確定／領収書／認定事業所）を全単元で修正。**U8 に s08〜s10 を追加**（ご希望3択／複数事業所まとめ見積もり／スタッフの見積もり作成・見積書表示）、旧 s08 まとめ→s11。`check_script.py` は U 系（出典JSONなし）では動かないため目視照合 | ✅ `ガイドブック動画_パイプライン/台本/U*.json` `.md`・`撮り直し一覧_20260914.md`・`shots利用一覧_20260914.md`・`U8_撮影メモ.md`・`読み辞書_追加候補_20260914.tsv`（Web→ウェブ） |
| スライド生成コード | 8単元の `素材/build_slides.py` の文言を新名称に。U8 に s08〜s10 の関数を追加 | ✅ 全単元ビルド通過 |
| 画面写真 | 本番CRM＋**サンプルデータ（Firestore 直書き・案件 #9008〜9011・EST-2026-0000/0001・SH-2026-0000）**で 33枚を撮影（`ガイドブック動画_パイプライン/撮影/shots_20260914/`）。Playwright MCP のログイン済みブラウザで、**`document.documentElement.style.zoom='2'` ＋ viewport 2480px で2倍解像度**の要素スクリーンショット（Playwright の DPR は 1 なので `scale:'device'` では等倍になる）。ネイティブ `<select>` の展開はDOMに偽リストを描いて撮る。モーダルは viewport を 3400px にして全体を撮り PIL で切る | ✅ 撮影済み。サンプルデータは撮影後に削除済み（0件確認） |
| 写真差し替え→スライド再生成 | 共通素材・各単元 shots を新写真に差し替え（旧は `_旧版_20260914/`）、CROP 調整、U8 プレースホルダー置換。司令塔の検収で追加修正: U2 s04 のタブ列を1行版（CSS 1600px 幅で撮影）に／U8 s09 の合成写真からヘッダー帯を除去／**`slide_kit.py` の `UNITS`（全単元の表紙に出る単元一覧）を8単元・「認定事業者」に**（旧7単元・「認定事業所」のまま残っていた）／U1 台本「全7回」→「全8回」・s08 に単元8の案内を追加／U1 まとめの「受発注・物品」→「お金と物品」／U8 s07「出荷を確定します」→「発送済にする」 | ✅ |
| 音声・合成・配布 | 旧 wav を全削除 → `tts.py build Un`（読み辞書に `Web→ウェブ` 追記）→ `assemble.sh Un` → 各単元フォルダ v1.2（U5 v1.3・U8 v1.1）→ 共有ドライブ `研修動画/CRM操作説明_全7単元/`（旧版 `_旧版_20260914/`・`00_索引.md` 作り直し）→ `sync-training-videos.py` で Storage → `video-catalog.js` 更新。尺: U1 5:25／U2 6:42／U3 5:59／U4 5:49／U5 3:40／U6 5:05／U7 6:17／U8 7:08（計 46:05）。字幕の旧名称 grep は全8本 0件。検品はフレーム3か所の目視 | ✅ Storage 同期済み（8 mp4＋8 vtt・05:00 UTC）。admin live へ反映（video-catalog.js・次田さん承認）。Issue #4 クローズ |

**Playwright MCP の DPR は 1**（`scale:"device"` でも等倍）。2倍解像度は `zoom=2`＋viewport 倍で撮る。`sk.UNITS` は全単元の表紙に出るので単元を足したら必ず直す。
**撮っていない・保留**: 単元3 s07（発注一覧の「直送（入荷なし）」・文言不変の見込み）／単元4 s08d（領収証メール送付ボタン・build から未参照）／単元5（撮り直し不要）。
**ハマり**: 撮影時に `shipments.receiptIssuedAt` を timestamp で書くと一覧が「発行済 tamp(second…」と崩れる（実装は文字列 `YYYY-MM-DD`）。サンプルデータは実装の型に合わせる。

## 見積もり Phase 3・複数事業所まとめ見積もり・LPフォーム「ご希望」3択（2026-09-14 実装・Functions 本番反映済み）

Fable 司令塔＋Sonnet 4体（C: LPフォーム 18万／Bf: mitsumori 34万／Af: CRM 22万／Wave2: Functions 23万トークン）。検収・通し確認・Codex レビュー対応は司令塔が直営。
決定（次田さん・同日）: ①複数事業所は**事業所ごとに案件＋見積書（EST番号も別）、`groupId` で束ねる** ②LPの「正式に申込みたい」は**見積もりツールへ誘導し発行後すぐ申込モーダル**（申込には見積書が必須）。

| # | 何 | どこ |
|---|---|---|
| C | LPフォームに必須ラジオ「ご希望」（consult／quote／order）。完了画面を出し分け（order は「見積書を作って申し込む」→ `mitsumori.html?t=…&intent=order`）。`cases.inquiryIntent`、Chat 通知の2行目に「ご希望: …」（🛒📝📥）。案件一覧に「ご希望」バッジ＋絞り込み、案件詳細ヘッダーにバッジ。⚠ ラジオは既存の `[required]` 判定から漏れる作りだったので個別判定に | index.html / webhookLpInquiry / constants.js / cases.js / cases.html / case-detail.js |
| B | 見積もりツール「＋同一法人の事業所を追加」（最大10）。共通＝法人名・担当者・電話・メール、事業所ごと＝事業所名・住所・電話・URL・プラン・構成。見積書は事業所ごと1枚＋法人合計表。`webhookMitsumori(offices[])` → `sendQuotePdf(pdfs[])`（1通に全PDF）→ `acceptQuote(quotes[], deliveryMode each/single)`。**1事業所の送信JSONは従来と同一**。`?intent=order` で発行後に申込モーダル自動オープン。1事業所処理は `createOrUpdateMitsumoriQuote` / `acceptQuoteTransaction` に関数化して両経路から呼ぶ | mitsumori.html / functions |
| A | CRM 見積もりカード「見積もりを作る」（callable `staffCreateQuote`・`createdVia:"staff"`・任意でテキストメール `noticeMailedAt`）。「見積書を表示」＝ `admin/quote-print.html` + `quote-doc.js`（見積もりツールと同じ体裁・印刷／PDF保存のみ／PDFを保存して送付＝callable `saveQuotePdf`）。PDF未保存の版でもここから作れる。内部ヘルパー `saveQuotePdf` は `storeQuotePdfFile` に改名 | quote-admin.js / quote-doc.js / quote-print.html / functions |

**検収で直したもの**
- Codex P1: 一括申込・一括見積もりで一部失敗しても `status:"ok"` を返していた → `partial`／`error` を返し、画面は受け付けた分と失敗した事業所名を分けて表示
- duplicate 応答の `groupId` が保存値とズレる（毎リクエスト採番していた）→ 既存版の groupId を返す
- 申し込み済みの案件に「見積もりを作る」が出る → 非表示＋サーバで `failed-precondition`（改版は「内容を変更する」）
- まとめ見積もりの兄弟案件が「重複の可能性」に出て統合を誘う → 同じ `quoteGroupId` は除外
- ⚠ 「メール本文の金額に桁区切りが無い」は**誤認**だった（Gmail MCP の snippet がカンマを落とすだけで、本文は「¥64,000」で正しかった）。その過程で金額整形を Intl 非依存の `fmtNum()`（正規表現）に統一済み（害なし・そのまま使う）。**メール書式の検収は `get_thread(PLAIN_TEXT)` の本文で行う**
- 見積書の右上ラベルが縦に折れる → `white-space:nowrap`／「お客様ご負担額」→「事業所さまご負担額（自己負担）」

**通し確認（本番 Functions・プレビュー Hosting・2026-09-14 10:45〜10:55 JST）**: LP（order）→ 完了画面 → 見積もりツール引き継ぎ → 事業所2件で「メールで受け取る」（EST-0023/0024・1通にPDF2枚）→ 申込モーダル自動オープン → 一括申込（SH-0019/0020・確認メール1通）／ CRM: 一覧の「ご希望」列・詳細のバッジ・新規案件 #159 で「見積もりを作る」（EST-0025・テキストメール）→ 見積書を表示 → 「PDFを保存して事業所へ送付」（Storage＋添付メール）。すべて成功。

**全件テスト（2026-09-14 11:15〜11:35 JST・本番 Hosting＋本番 Functions・次田さん指示）**

| # | 経路 | 結果 |
|---|---|---|
| T1 | LP「まず相談したい」→ 完了画面 | ✅ 従来表示（見積もりリンクは控えめ）・`inquiryIntent:consult` |
| T2 | LP「見積書がほしい」→ 完了画面（主ボタン）→ 見積もりツール引き継ぎ（token）→ 1事業所「PDFでダウンロード」→ 申込（前払い） | ✅ 送信JSONは従来のフラット形・`window.print` 1回・メールなし（pdfPath null）・申込完了メール1通・出荷下書き1件 |
| T3 | 見積もりツール直接・2事業所（訪問／居住・USB Type-A）・「PDFでダウンロード」・お届け先「1か所へまとめる」 | ✅ 案件2・見積2（groupId 同一）・出荷下書き2件とも届け先＝本部住所・確認メール1通 |
| T4 | 一括申込の一部失敗（2件目のトークンを不正に） | ✅ サーバ `status:"partial"`＋`results[].error`／画面「事業所 1件／1件は受け付けられませんでした・受け付けられなかった事業所: G事業所」 |
| T5 | CRM「見積もりを作る」（メールなし）→「内容を変更する」（BT3＋USB追加1 Type-C・メールあり） | ✅ v2 発行・v1 旧版・¥70,500／自己負担 ¥6,500・変更後メール（差額 ＋¥6,500） |
| T6 | 期限切れ（validUntil を過去に）→ カードの帯・見積書の帯 | ✅ 両方に表示 |
| T7 | スマホ幅 375px で事業所ブロック追加 | ✅ ブロック自体は収まる。⚠ ヘッダーの2ボタンが 729px まではみ出す＝**変更前からの既存事象**（ヘッダーCSSは今回未変更） |
| T8 | **助成金3プラン × 補助対象外（追加）台数のマトリクス**（次田さん指摘で追加・11:35〜11:40）: H=訪問 BT3＋BT追加2／I=居住 BT1＋USB1(A)＋USB追加1／J=その他 USB1(C)＋BT追加1 を1グループで「メールで受け取る」→ 一括申込 | ✅ 画面・サーバ・出荷明細が三者一致。H ¥93,000（自己負担 ¥29,000）／I ¥61,500（¥6,500）／J ¥56,500（¥14,500）・法人合計 ¥211,000（助成金 ¥161,000・自己負担 ¥50,000）。出荷明細は H: CIR415A×5＋伴走支援費(3台)50,000＋金額調整−29,500／I: CIR415A×1＋CIR315A(A)×2＋55,000−21,000／J: CIR415A×1＋CIR315A(C)×1＋60,000−24,500。PDF3枚を1通で送付（Storage 3件・gmail_sent 記録あり） |
| 回帰 | 案件詳細の重複候補（同一法人グループ）／「ご希望」バッジ／一覧の絞り込み | ✅ グループ兄弟は重複候補に出ない・バッジ表示・絞り込み5択 |

テストデータ（案件 #160〜166・EST-2026-0026〜0032・SH-2026-0021〜0026・事業所7・記録・leadToken 2・Storage PDF 3）は削除済み（0件確認）。
追加で直したもの: 見積もりツールの `input[type=tel|url|email]` にスタイルが当たっておらず細かった → 追加（lp live `6a453812c0183508`）。

**小さな宿題は 2026-09-14 15:0x に5件まとめて解消**（commit `2a046e4`・lp live `d4b088ff3e7b254e`・admin live `3a3def7272b51db3`・Functions `reviseQuote`）: 改版時に有効期限を30日振り直す／LP完了画面の先頭文をご希望で出し分け／CSVに「ご希望」列／見積もりツール 560px 以下でヘッダー見出しを畳む／`splitCity` を `js/split-city.js` に共通化。**据え置き**:  `leadTokens.usedFor` に "order" を積んでいない（単一経路も同じ・実害なし）／LP完了画面の「2営業日以内にご連絡」が order でも出る（文言の整理）／CSV出力に `inquiryIntent` 列なし／複数事業所のPDFダウンロードは `window.print()` 一括（事業所ごとの分割保存はブラウザの印刷ダイアログで）。

## 現在の状態（2026-09-14 朝・前回終了時）

- LP（https://kjk.tadakayo.jp）・CRM（https://kjk-tadakayo-admin.web.app）とも本番稼働中。リポジトリ `npo-tadakayo/kjk-tadakayo`、最新 commit `ec7ee70`、admin live `2b3c7edec0168d71`、lp live `4828f4e0488c0262`。**未コミット0・stash 0・push 済み**。
- **この2日で本番に入ったもの**（各節に詳細）: 保管場所ごとの在庫（滋賀167／沖縄47）／消費税の二重計算の是正（事業所＝税込・卸＝税別）／帳票の書式（領収証・発行元・説明行・担当＝案件の担当営業）／請求書メールの宛先自動入力と支援報告書の同封／UI改善A（メニュー再編・今日やること・1行説明）・B（出荷・請求・入金タブ・ボタン文言・§0業務の流れ）・C（次の一手・3ステップ案内）／出荷のキャンセル／firestore.rules（本人の `onboardingDismissedAt` のみ）。
- **文書**: `docs/機能一覧_20260914.md`（486行・§11.3 で要確認5件すべて決着）。画面内マニュアル `admin/manual.html` は最新。**配布用PDFと動画8本は未更新**（下の「次回やること」1）。
- **次田さんの決定**: 「システムを最終形にしてから、マニュアルPDFと動画をまとめて直す」。予定していたシステム側の変更は**すべて本番に入った**ので、次回はその着手判断から。
- 認可ドメインは本番4件のみ（プレビュー用は検証のたびに足して外す）。Firestore に **本番テストデータは残していない**（キャンセルのモーダルは開いて閉じただけ）。
- 請求はいつでも出せる状態: 下書き4件（¥64,000×3・¥42,000）、ケアプラン愛 SH-2026-0015（¥43,500・宛先自動・担当 次田）。**担当営業が空**: SH-2026-0012（案件に未割当）・SH-2026-0011（案件が見つからない）→ 請求書の「担当」が空欄になりアラートが出る。

## 保管場所ごとの在庫管理と出荷元の指定（2026-09-12・本番反映済み）

**仕入れた在庫の置き場所が滋賀だけでなくなったため**、どこに何台あるか・どこから出したかを
管理できるようにした（次田さん要望）。admin live `500e0e9b39c0cbd7` ／ commit `e2b39d9`

| 何 | どうした |
|---|---|
| 保管場所マスタ | 新コレクション `locations`（名称・都道府県・使用中）。在庫タブから追加・編集。**増えたら足すだけ** |
| 場所ごとの在庫 | `products.stockByLocation`。**合計の `stock` は残した**（既存の表示・在庫判定・請求がそのまま動く） |
| 出荷 | `shipments.originLocationId`。発送済の確定でその場所から引く |
| 入荷 | 確認ダイアログ → **入荷先の保管場所を選ぶモーダル**に変更。`purchaseOrders.receivedLocationId` |
| 郵送なし | 配送方法に「郵送なし（手渡し・持参）」を追加（送料0） |
| 送料 | ゆうパックの発地表記を保管場所の都道府県に連動。滋賀以外なら注意書きを出す |

🔴 **在庫を動かすときは必ず `stockPatch()` を通すこと。** 合計と内訳を必ず一緒に動かすための入口で、
在庫を触る6か所（在庫調整・入荷・出荷確定・出荷修正・出荷削除・受注からの出荷）を全部ここに集約した。
`{stock:increment(...)}` を直接書くと**合計と内訳がずれて、どこに何台あるか分からなくなる**。

- 保管場所を作る前からある在庫は「**未割当**」として赤字で出る。在庫調整の「この場所に割り当て」で
  **合計を増やさずに**場所へ移せる（移行用。普通の入庫と混同しないこと）
- 出荷に `originLocationId` が無い過去データは、既定の保管場所から出したものとして扱う（`shipLocationOf()`）

### 実機で確かめたこと（プレビュー・2026-09-12）
保管場所の登録／未割当214台から10台の割り当て（**合計が214のまま**）／在庫一覧の内訳／保管場所の在庫合計／
出荷モーダルの発送元／「郵送なし」で送料0／ゆうパック表記が「滋賀県発」に変わること。
テストで発見1件（保管場所の在庫合計が更新されない＝products の購読で `renderLocations()` を呼んでいなかった）→ 修正済み。
**テストデータは削除済み**（在庫214台・locations 0件をAPIで確認）。

### まだ確かめていないこと
本番データが動くため、**出荷確定・入荷登録・出荷の修正/削除での在庫増減は実機未確認**。
次に実際の出荷・入荷が発生したときに、合計と内訳の両方が動いているか確認すること。

### 次田さんにお願いすること
1. 実際の**保管場所を登録**する（名前・都道府県。都道府県はゆうパックの発地に使う）
2. **未割当の214台を各場所に振り分ける**（在庫調整 →「この場所に割り当て」。合計は変わらない）

## 初見でも使えるように — 改善C ＋ 出荷のキャンセル（2026-09-14 実装・同日 本番反映済み・rules も反映）

**段取りの決定（次田さん・2026-09-14）: システムを最終形にしてから、マニュアルPDFと動画8本をまとめて直す**（手戻りを1回に）。
Fable 司令塔＋Sonnet 3体（C1/C2/キャンセル・消費 約48万トークン）。検収・プレビュー検証（`uxc-0914`）は司令塔が直営。
commit `5129b84`（C1/C2/rules）・キャンセルは本節と同じコミット。

| # | やったこと | 主なファイル |
|---|---|---|
| C1 | 案件詳細ヘッダー直下に**「次の一手」**。ステータス→次にやること・飛び先の表 **`NEXT_ACTION`** を constants.js に置き（マニュアル §0 と同じ内容を指す）、1行＋主ボタン1つ。未完了の出荷があれば「お金の流れ：次は…」を1行（B1 の `shipNextAction` を共通化・購読は1本）。完了13・失注4は非表示。タブ切替は `activateTab()` に共通化 | constants.js / case-detail.html / .js |
| C2 | ホームの「今日やること」の上に**「はじめての方へ — 3ステップ」**。「わかりました」で `users/{自分}` に `onboardingDismissedAt` を merge 保存（端末を変えても再表示しない）。読めない・書けないときは出さない側 | dashboard.html / .js |
| rules | `users/{email}`: **本人が自分の文書の `onboardingDismissedAt` だけ書ける**例外（`affectedKeys().hasOnly`・値は `request.time` 固定）。他は従来どおり管理者のみ。⚠ **Hosting とは別に `firestore:rules` のデプロイが要る**。users 文書が無い人は「作成」扱いで拒否されるが、職員14名全員に文書があり、無いとログイン自体が通らないので実害なし | firestore.rules |
| キャンセル | 出荷ステータス `canceled` に遷移する操作が無かった（機能一覧 §11 要確認1）→ **B「キャンセルにする」を追加**（次田さん判断）。draft/shipped/invoiced に表示、paid は返金フローに任せて出さない。理由（事業所の都合／重複登録／誤登録／その他）＋一言を保存、`statusBeforeCancel` も残す。**在庫の戻しは shipped/invoiced かつ自社在庫のときだけ**、`stockPatch`/`movement`/`shipLocationOf` 経由（draft・直送は動かない）。**「この出荷を削除」は draft のみ**に限定。キャンセル済みの行は薄く・ボタン無し・理由を表示。ホームの集計は canceled を既に除外 | supply.js / supply.html |
| 要確認2〜5 | すべて決着（`docs/機能一覧_20260914.md` §11.3）。`shipFeeIncl`→`hasShipFee`（`17aff6a`） | invoice-doc.js |

🔴 **プレビューのログインは、認可ドメインを足した直後は1回目が空振りする**（反映待ち）。2回目で通る。失敗＝壊れた、ではない。

### 次（システムが最終形になったら）
1. **配布用マニュアルPDF**（`00_outbox/介護情報基盤_資料一式_20260902/10_…`）を `admin/manual.html` から出し直す
2. **CRM動画8本の再生成**：順序 1 → 3・8 → 4 → 2 → 6 → 5・7（台本 U1〜U8 は `06_介護情報基盤事業/ガイドブック動画_パイプライン/台本/`。内容の誤りは無く名称・画面の差だけ。単元4の税のセリフは今も正しい）
3. マニュアル §0「この段階でやること」と `NEXT_ACTION`「次にやること」は**意味が違う**（現在の段階 vs 次）ので文言が一致しなくてよい。C1 担当が挙げた5件の差分は放置で問題なし

## 初見でも使えるように — 改善B（2026-09-14 実装・同日 本番反映済み）

A の続き。Fable 司令塔＋Sonnet サブエージェント4体（B1/B2/B3/機能一覧・消費 約99万トークン）。
検収・統合・プレビュー検証（`uxb-0914`）は司令塔が直営。commit `33fa945`（B1/B2/CSS）・`7716159`（機能一覧）・B3 は本節と同じコミット。

| # | やったこと | 主なファイル |
|---|---|---|
| B1 | 案件詳細に**「出荷・請求・入金」タブ**（書類チェックと申請情報の間）。その案件の出荷を一覧し、4段のステッパー（発送済→請求書→入金→領収証）と**「次にやること」を1つだけ**主ボタンで示す。**金額を動かす操作は複製しない**（`?so=` で該当行へ・帳票は PDF ページへ飛ばすだけ）。金額は `invoiceTotals` 経由。案件詳細のボタン文言も追従（モーダルの確定ボタンまで揃えた） | case-detail.html / .js |
| B2 | 出荷一覧のボタン文言を**「動詞＋結果」**に（発送済にする／入金を記録する／送付先・数量を直す／請求先を変える／この出荷を削除…。「取消」は入金・返金の種別で出し分け）。行内ボタンを**左から順に押せば進む**並びに。表示条件・動作・金額計算は不変。`?tab=shipments&so=SH-…` で該当行へスクロール＋3秒強調（最初の描画で1回だけ） | supply.js / supply-print.html |
| CSS | 文言が長くなり**ノートPC幅（約1470px）で操作ボタンが横スクロールの外へ163px 押し出されていた**。出荷一覧だけ、送付先・内容・状態列を折り返し可にし、操作ボタンを2列グリッドに → はみ出し0を実測 | crm.css（`#shipBody` 限定） |
| B3 | マニュアル冒頭に **§0 業務の流れ**（5段階×13ステータス＋失注：やること／使う画面／押すボタン／出る書類）＋表2 お金の流れ。ボタン名・画面名を最新に。「認定事業所→認定事業者」の古い告知は §4 FAQ へ。**動画内のボタン名は撮影時のもの**と1行注記 | manual.html |
| 機能一覧 | `docs/機能一覧_20260914.md`（486行）。コードを根拠に画面19／機能130行／帳票8種／Functions 13本／設定14項目／ステータス13。§11 に実装途中4・要確認5・古い名称の残存2を行番号付きで | docs/ |

契約（依存関係）: 案件詳細タブ →「出荷・請求・入金」の行へは **`supply.html?tab=shipments&so={soNumber}`**。ホームの「今日やること」→ `?tab=` のみ。

⚠️ 教訓: **ボタン文言を長くすると表がはみ出す**。文言変更のときは実機の幅（1470px 級）でスクリーンショットを撮る。
残件（次の候補）: 「次の一手」ガイド（C1）／初回ログイン時の3ステップ案内（C2）／機能一覧 §11 の要確認5件の潰し込み。

## 初見でも使えるように — 改善A（2026-09-13 実装・2026-09-14 本番反映済み `4b2c252`）

次田さん「業務の流れと操作メニューが分かりづらい。初見でも使えるようにしたい」。
根本は**メニューが業務の流れではなくデータの種類で切られていた**こと。まず小さく効く3点（A）を入れた。
プレビュー `ux-0913` で全項目を実測確認。**Fable を司令塔にし、実装は Sonnet のサブエージェント3体に分業**
（A1/A2/A3・消費 約54.5万トークン）。検収・統合・検証は司令塔が直営。

| # | やったこと | 主なファイル |
|---|---|---|
| A1 | メニューを業務の流れの順・業務の言葉に。**「供給管理」を「出荷・請求・入金」「在庫・発注」の2入口に**（同じ supply.html を `?tab=` で開くだけ・ページ分割なし）。開いたタブに合わせて見出し・`<title>` も変わる。タブごとに1行説明 | sidebar.js / supply.js / supply.html / manual.html |
| A2 | ホーム（旧ダッシュボード）の先頭に**「今日やること」**7行（新規問い合わせ／担当営業未割当／伴走支援待ち／出荷下書き／未請求／未入金／領収証未発行）。件数＋「開く」。0件も「なし」で残す＝チェックリスト | dashboard.html / dashboard.js / cases.js（`?status=` `?assigned=none`）/ constants.js（`ENGAGED_STATUSES` を SSOT 化）/ crm.css |
| A3 | 全画面のタイトル直下に**1行説明** `.page-intro`（crm.css に1回定義）。**画面ごとに主ボタン（.btn-primary）を1つ**に | cases / kanban / case-detail / partner-admin / pricing / simulator / settings / users |

契約: **`supply.html?tab=inventory|orders|shipments|partnerorders|partners`** でそのタブを開く（ホームのリンクと帳票ページの「戻る」が依存）。
サイドバーの強調は `tab` まで見る（inventory/orders→在庫・発注、それ以外→出荷・請求・入金）。

⚠️ ハマり: **Hosting の cleanUrls で実パスは `/supply`（.html なし）**。`"/supply.html"` を含むかで判定すると
一致せず強調が消える。正規表現 `/(^|\/)supply(\.html)?$/` で判定する。**プレビューのスクリーンショットで気づいた。**

次（B・未着手）: 案件詳細に「お金の流れ」タブ／ボタン文言を「動詞＋結果」に／マニュアル冒頭に業務の流れ1枚。

## 🔴 消費税の二重計算を是正（2026-09-13・本番反映済み）

**事業所向けの請求書・領収書が、税込の金額に さらに10%を上乗せしていた。**
次田さんが「伴走支援を提供した事業所へ出す領収書の書式」を持ってきたことで発覚。
admin live `8d489f0267d687f9` ／ commit `f231c16`

### 何が起きていたか

見積もりの価格は **すべて税込**（`js/estimate-pricing.js` の `BT_PRICE = 14500 // 税込`・
`ACCOMPANY_FEE` も税込）。見積書にも「金額はすべて税込」と明記している。
Web申込から作られる出荷明細にも **その税込の数字がそのまま** 入る
（`functions/index.js` の `unitPrice = Pricing.BT_PRICE`）。
ところが請求書・領収書・一覧はこれを **税抜** として集計し、10%を足していた。

| 3台構成 | 見積書（正） | 修正前の請求書 | 修正後 |
|---|---|---|---|
| 合計 | **¥64,000**（助成金上限ちょうど） | ¥70,400 | **¥64,000**（うち消費税 ¥5,818） |

上限 ¥64,000 を超えると「**自己負担ゼロ**」の料金設計（`PRICING.md`）が崩れ、
事業所に ¥6,400 の負担が出るところだった。**誤った金額の請求書は1通も送られていない**
（事業所向け4件とも `invoiceMailedAt` 未設定。※CRM経由の送付記録上）。

### 直し方（ここを崩さない）

🔴 **明細の単価が税込か税抜かは請求先で変わる。** `invoice-doc.js` の `priceIsTaxIncluded()` が正本。

| 請求先 | 単価 | 扱い |
|---|---|---|
| 事業所へ直接販売（`direct`） | 定価＝**税込** | 合計から割り戻す（`floor(合計×10/110)`） |
| 認定事業者への卸（`dropship` / `partnerEmail` あり / 旧 `stock`） | 卸価格表＝**税別** | 従来どおり小計に10%を足す |

- 判定は `supply.js` の `billsPartner` と同じ。**`shipType` の文字だけで見ない**
  （旧 `stock` 型の SH-2026-0001 も `partnerEmail` があるので卸側に寄る）
- 金額計算は **`invoiceTotals` に一本化**した。supply.js の自前計算（`shipTotalIncl` ほか）と
  supply-print.js の `billableInclOf` は委譲に変更。**今後ここに自前の税計算を書かない**
- 請求書・領収書の列見出しと合計欄を税基準で出し分け（税込なら「税込小計／うち消費税(10%)」）
- **領収書の用途区分を品番から自動で振る**ようにした。以前は全部Aだったため、
  伴走支援費も値引きもカードリーダー費に混ざり、**助成金の申請対象額の内訳がずれていた**
  （いま: A ¥43,500 ／ B ¥20,500 ＝ 申請対象額 ¥64,000）
- 見本に合わせ **宛先の住所**と**関連請求書番号**を記載

### 検算（本番実測）

| 対象 | 結果 |
|---|---|
| SH-2026-0013（事業所・3台フル） | ¥64,000／うち消費税 ¥5,818・登録番号 T8010805002793 あり |
| SH-2026-0015（事業所・3台のみ） | ¥47,850 → **¥43,500** |
| SH-2026-0001〜0004（入金済みの卸） | 合計・消費税とも **変化なし**（4件確認） |

**インボイス登録番号は本番に入れる**（2026-09-13 次田さん指示）。設定に
`invoiceRegNo = T8010805002793` が入っており、請求書・領収書の両方に印字される。
⚠️ いただいた見本には登録番号が無かった。**見本の方を直す**こと
（無いと受け取った事業所が仕入税額控除を使えない）。

### 見本への書式合わせ（2026-09-13 完了・本番反映済み `5dd7316f04bde1c3` ／ commit `d94d0ee`）

- **発行元の連絡先**を設定に追加（`invoiceIssuerPostal/Address/Tel/Contact`）。請求書・領収証の
  発行元欄に印字。**入れた項目だけ出る**ので未設定なら従来どおりの表示
  → 本番の設定値: 〒143-0014 東京都大田区大森中2-1-20-1001 ／ TEL 050-6872-9884 ／ 担当 藤田 博之
  （見本に記載の値。担当者は発行する人で変わるので、変えるときは設定画面から）
- **品名の下に用途の説明**を出す。正本は `invoice-doc.js` の `itemPurposeNote()`
  （カードリーダー＝マイナ資格確認アプリ対応／伴走支援費＝導入設定・…・1年間サポート）。
  助成金の申請で「何に使うものか」が書面から分かるようにするため
- **表題を「領収証」に統一**（利用者に見える箇所のみ。エンジニアノートとコード内の
  コメントは経緯が追えるよう「領収書」のまま残した）
  ⚠️ 表題は全角スペース入りの **`領　収　書`** で、最初の一括置換から漏れていた。
  **文字列の一括置換は、実物のスクリーンショットで確かめること**（grep だけだと気づけない）

## 請求書メールの宛先自動入力と、支援報告書の同封（2026-09-12〜13）

ケアプラン愛様へ請求書を出す場面で分かった不足に対応した。

### ① 宛先の自動入力 — **本番反映済み**（admin live `3d87eb2b16cf90b6` ／ commit `0e12b60`）

事業所へ請求するとき、宛先が空欄で出てきて毎回手入力していた（画面が
**認定事業者のメールしか見ていなかった**）。

- 出荷に「送付先メール」欄（`email`）を追加。案件から出荷を作ると**案件の担当者メールを
  引き継ぎ、案件ID（`caseId`）も出荷に残す**
- 請求書・領収書のメール画面で、宛先を
  **「認定事業者 → 出荷の送付先メール → 案件の担当者メール」**の順に自動で入れる
- 入ったあとも画面で直せる（先方の担当が変わることがあるため）

### ② 支援報告書の同封 — **画面は本番未反映**（commit `077b019`）

「請求書と一緒に作業の報告も渡したい。送信前に選べるとよい」（次田さん）。

| 層 | 状態 |
|---|---|
| Functions `sendPartnerMail` | **本番反映済み**。追加の添付（`extraAttachments`・最大3つ）を受け取る。合計10MBの上限は帳票と同封ぶんを足して判定。**後方互換**なので今の本番画面は壊れない |
| 画面（admin） | **本番反映済み**（2026-09-13・`8d489f0267d687f9`）。消費税の是正を本番に出した際、admin は全ファイルまとめて配信されるため同時に出た |

- 支援報告書に `mode=client` を追加。事業所へ送る版では**社内の対応履歴（電話・メール・メモ）を載せない**（2026-09-12 次田さん判断）。伴走支援の記録と事業所情報は載る
- 請求書のメール画面に「支援報告書も同封する」を追加。**案件が紐づいた出荷のときだけ**出る
- 報告書は画面外の iframe で `report.html` を開き、**その中で** PDF 化する
  （別ページのスタイルのまま正しくPDFにするため。親から `contentWindow.__reportPdf()` を呼ぶ）
- プレビュー実測: PDF生成 **1.1秒・約207KB**／対応履歴が載っていないことを確認

⚠️ **未確認: 実際にメールを送って添付が2つ届くか**は試していない（本物の事業所へ送ることに
なるため止めた）。確かめるなら**次田さんのアドレス宛にテスト送信**するのが安全。
**メール送信は次田さんの明示承認が必要。**

### データの現況（2026-09-12 時点）

- CIR415A 214台 ＝ **ロジスティックセンター（滋賀）167台 ／ スマイルプラス（沖縄）47台**
  （沖縄は「50台保管」から当日3台出荷した後の残り＝次田さん確認済み）
- 今日の出荷 **SH-2026-0015**（ケアプラン愛・CIR415A×3・発送済）に
  **発送元＝スマイルプラス（沖縄）**を記録。在庫は出荷時に引かれ済みのため動かしていない。
  在庫ログにも場所を補記した
- 同出荷に `email`（案件の担当者メール）と `caseId` を補完済み＝宛先が自動で入る状態

### 認可ドメインの掃除（2026-09-13 実施済み）

Firebase Auth の認可ドメインを **15件 → 5件**にした。プレビュー用のチャンネル（ホスト名に
`--` を含むもの）10件をすべて削除し、本番の5件だけ残した。

| 残したもの |
|---|
| `kjk-tadakayo.firebaseapp.com` ／ `kjk-tadakayo.web.app` ／ `kjk-tadakayo-admin.web.app` ／ `kjk-tadakayo-admin.firebaseapp.com` ／ `admin.kjk.tadakayo.jp` |

⚠️ **プレビューでログインして検証したいときは、その都度そのホスト名を足して、終わったら外すこと。**
足したまま放置すると今回のように溜まる（判定は「ホスト名に `--` が入っていたらプレビュー用」）。

## 回答を待っていること（基盤スペースで報告済み・2026-09-07）

報告に対する返事が来たら、この順で進める。**こちらから催促しない。**

| # | 待っていること | 返事の内容で分かれるもの |
|---|---|---|
| 1 | <s>**Formspree の扱い**</s> → **2026-09-09 に送信を止めた**（通知はタダカヨのサーバーから事務局メールへ）。報告書 `docs/PMS報告_…_20260908.md` §0 に結果を追記済み | 残るのは **Formspree 側に残る過去データの削除依頼**と、**残存件数の確認**（管理画面へのログインが必要） |
| 2 | **zipcloud の扱い**（郵便番号→住所の自動入力・閲覧者のIPが外部へ） | (a)ポリシーに明記 (b)Functions 経由に変える (c)自動入力をやめる。⚠ 2026-09-14 の複数事業所対応で、見積もりツールの各事業所ブロックにも同じ自動入力が付いた（`wireZipAutofill` を各ブロックに配線）。(b)(c) なら LP・見積もりツール共通の `js/split-city.js` 周りと各ブロックの配線を直す |
| 3 | Microsoft Clarity・GA のポリシー記載の有無 | 記載が無ければ追記 |
| 4 | <s>介護情報基盤の部門保護管理者はどなたか</s> → **蜂須賀さん**（2026-09-08 次田さん回答・確定） | PMS報告の宛先はこの方 |
| 5 | 認定マーク中央の「2026」の扱い | そのまま／「2026年度」／外す |
| 6 | 認定書類3点の文言確認 | 確定後に様式を固定する |

## 次回やること（優先順・2026-09-18 更新）

**システム側でやることはほぼ残っていない。下の1〜3は次田さんの操作・判断待ち。**

1. **請求まわり（次田さんの操作）**: ケアプラン愛 SH-2026-0015 は金額修正済み・メール送付済み → 赤い「請求済にする」を押すだけ／下書きの請求書4件の発行／SH-2026-0012 に担当営業を割り当て／SH-2026-0011 は案件が消えているので出荷に案件を紐づけ直し。支援報告書の同封は**次田さん宛のテスト送信で添付2つを確認してから**
2. **経理への請求書発行報告の実機検証**（5分・上の請求1件で「経理へ報告する」ON）
3. **PMS・回答待ち**（催促しない）: Clarity のポリシー記載／zipcloud／Formspree 残存データの削除依頼／認定マーク「2026」／認定書類3点の文言／見本の領収証に登録番号を追記（次田さん側）／**PMS報告書を蜂須賀さんへ**
4. **アークメディカル 髙井様**（別件・`06_介護情報基盤事業/outbox/20260911_髙井様_…md` 下書き未送信）: 候補日3つ → 返信、CRM 重複6件を #90 に寄せて受注確定、担当割当、メール受領の申込を CRM に入れる運用
5. **残っている実装**: 認定事業者の書類サンプル（Issue #5・§6 の回答後）／CRM操作の追加動画3本（担当営業の割当・紹介元・統合）／**合計お見積もりのご案内を CRM から自動生成**（いまは手元の JSON を書いて `scripts/quote-cover/` で生成。法人まとめ見積もりの groupId 単位で1本出すのが素直）
6. 小さな宿題: 請求書の金額調整表記「¥-29,500」→「−¥29,500」（見た目のみ）／動画の写真2枚は旧画面のまま据え置き（単元3 s07・単元4 s08d）／PO-2026-0049 の入荷登録（届いたら）／**HANDOFF が 400行超 → 古い節を `HANDOFF_ARCHIVE.md` へ**
（運用）CIR415A は半年に1度の通電をアナウンス（2026-07-24 林さん）

## 未決事項

（2026-08-12 に3件すべて実装・本番反映して解消。下の「重要な合意事項」へ移動）

## 重要な合意事項（蒸し返し禁止）

- **認定事業者制度の運用方針**（2026-09-05 MTG・藤田/次田/蜂須賀）: スタッフ全員を一律にタダメンにしない／代表者または主担当者1名以上はタダメン加入／その他の実務担当者はゲストアカウント（1年更新）／理念の説明→共感→加入→認定手続きの順序／案件ごとに「タダメンとして」か「認定事業者として」かを選べる／自社対応分も**支援件数だけ**は報告してもらう／**2026年度はプロトタイプ運用で積極募集はしない**。議事録: Google ドキュメント「介護情報基盤 認定事業者・新タダメン運用相談MTG 議事録」
- **見積もり後の内容変更を容認する**（2026-09-06 次田さん）: 台数・BT/USB・Type-A/C・機種の変更を、支援の調整時にスタッフが CRM で行える設計にする。見積もりは版（version）で積み、出荷確定までは改版、確定後は供給管理の「出荷の修正」で。詳細 `docs/問い合わせ・見積もり・注文の統合_実装計画_20260906.md` §2.1
- **赤の正本は `#E33535`（タダカヨレッド）＋ `#FFE4EC`（ピンク）**。白文字ボタン・ピンク面の文字は `#c02828`
- 認定事業所卸は `appConfig.settings.partnerPricing`（非パススルー）。**CIR415Aは ¥8,000 固定**（2026-07-28）
- **送料は税抜で保存・入力**（発注・出荷とも）。請求書は税抜で積み上げ、消費税は小計に1回だけ
- **AB Circleの送料は1便100台以上が無料**（地域別は北海道¥1,500／関西¥1,100 ほか＝`supply-pricing.js`）
- **入金・未集金の管理単位は出荷(SH)単位**（月締めでまとめる方式は採らない・2026-07-30 次田さん）
- **過入金は返金より「次回請求から差し引く」が基本**（返金も選べる・2026-08-01 次田さん）
- 領収書の**明細編集**は印刷用の一時編集。ただし**発行記録**（番号・発行日・金額・明細）は `receipts` に保存する（2026-07-30 変更）
- **ICカードリーダーの供給・請求はこのCRMが正本**。ケアプー記録アプリには実装しない
- **過入金の充当は「充当元を人が選ぶ」方式**（2026-08-12 次田さん）。同一請求先は既定チェック＋古い順に自動配分＝従来と同結果。**別請求先（グループ会社）も選べるが既定はオフ・確認ダイアログ必須・自動では絶対に跨がない**。跨いだ場合は請求書に充当元の請求先名を印字する
- **返金の証憑は「返金明細書」**（`type=refund`・番号 RFND）。領収書ではなく当方が支払う側の書面なので印紙は不要。返金内容の正本は `shipments.refunds[]` で、別コレクションは作らない
- **領収書の領収金額は実入金から自動**（2026-08-12）＝`min(純入金, 請求額(充当後))`。過入金の超過分は含めない／一部入金は但し書きを「内金として」に自動変更／充当分は書面に注記。金額は画面で上書き可

## ハマりポイント・注意事項

- **ログインが要る CRM 画面を画像ファイルにする方法（2026-09-06 確立）**: Claude in Chrome の `javascript_tool` で html2canvas を読み込み、要素範囲を `scale:2` で描画 → **Firebase Storage `reports/{name}` に `uploadBytes`**（storage.rules で @tadakayo.jp が書ける）→ `gcloud storage cp` で取得 → 即 `rm`。`<a download>` はダウンロード確認ダイアログで止まり、`http://127.0.0.1` への fetch は **Chrome の Private Network Access で応答なし（45秒タイムアウト）**になるので使わない。一覧は撮る前に DOM のテキストノードをサンプル値へ置換し、正規表現で漏れ0を確認。**モーダルは背景の一覧が縁に写り込む**ので切り出しでモーダル本体だけにする
- **`mitsumori.html` の引き継ぎと郵便番号自動入力の順序**: `loadLeadToken()` が `contactZip` に値を入れて `input` を発火すると `wireZipAutofill` が zipcloud を引き、`city` を「◯◯市」に上書きする（区は住所欄が空のときだけ補われる）。引き継ぎ時は `contactZipAutofill.markLookedUp(digits)` で検索済み扱いにしてある。**注文モーダルの `set()` は input を発火しない**ので同じ問題は起きない
- **本番の通し確認は自動通知・メールが実際に飛ぶ**（webhookLpInquiry／webhookMitsumori／acceptQuote／reviseQuote の Chat 通知、事業所宛メール）。宛先は自分のアドレスにし、事業所名に【削除予定テスト】を付け、終わったら cases/quotes/shipments/offices/activities/leadTokens/Storage `quotes/{caseId}/` を REST で削除する（`scratchpad/fs_query.sh` のような runQuery ラッパーで残存確認）

- **html2canvas（html2pdf）は `@media print` を見ない**。画面の DOM をそのまま渡すと `.rcpt-noprint` や `<input>` が PDF に写る。帳票のメール添付は `printableClone()`（supply-print.js）で複製を整えてから渡す（2026-09-06）
- **html2pdf は「そのときの画面幅のレイアウト」をそのまま写す**（2026-09-06 見積書で発生）。`mitsumori.html` の `.est-doc` は `@media (max-width:820px)` で `width:100%` になるため、狭い窓でPDF化するとA4に収まらず左が切れて2ページになった。**A4幅をインラインで固定した複製**（`quotePrintableClone()`・インラインは @media より優先）を渡して解決。`html2canvas.windowWidth` を渡してもCSSのメディアクエリ評価は変わらないので効かない
- **プレビューチャンネルでログインするには Firebase Auth の承認済みドメインに追加が必要**（localhost も未登録）。`PATCH identitytoolkit.googleapis.com/admin/v2/projects/kjk-tadakayo/config?updateMask=authorizedDomains`（ヘッダー `x-goog-user-project: kjk-tadakayo`）。既に登録済みのチャンネル名（`sess-edit` 等）を再利用すると手間が省ける
- **Functions デプロイは `NODE_OPTIONS=--max-old-space-size=8192 FUNCTIONS_DISCOVERY_TIMEOUT=180`** を付ければ Drive 上からでも通る（node_modules 読込 58秒・既定10秒で落ちる。2026-09-06 は CLI が Node ヒープ不足で落ちたのでヒープも広げる）。`zsh -ic 'gcp tadakayo && …'` で1回にまとめる。**成否は `gcloud functions describe … --format="value(updateTime)"` で確認**（`| tail` を挟むと exit code 0 に見える）
- **Playwright MCP のブラウザ（user-data-dir `~/Library/Caches/ms-playwright-mcp/`）に CRM のログインセッションが残っている**（2026-09-06・次田芳尚でログイン）。実機テストはまず `browser_navigate` で本番 URL を開いて試す。ログイン画面が出たら Google のポップアップで次田さんがサインインする必要がある（Claude は資格情報を入力しない）
- **AppleScript から Chrome に流す JS は分離ワールド**（ページの `window` が見えない）。ページの値が要るときは inline `<script>` を注入して `document.body.dataset` 経由で受け取る。ログイン済み窓が無いときは Playwright MCP も Google 認証を通せない（資格情報入力は不可）→ **ローカルハーネス＋Firestore エミュレータ**で検証する（2026-09-06 実施。ルールの境界テストは `firebase emulators:start --only firestore --project demo-kjk` に REST で当てる）
- **Functions デプロイは Drive 上から不可**: `functions/node_modules` の読込に **12分23秒**（ローカルなら0.1秒）かかり、Firebase CLI の10秒制限に間に合わない（`Cannot determine backend specification. Timeout after 10000`）。手順は ①`rsync -a --exclude node_modules functions/ <ローカル>/functions/` ②`.firebaserc` コピー＋`firebase.json` は `{"functions":{"source":"functions"}}` ③`npm ci --omit=dev` ④`deploy --only "functions:名前" --account yoshinao-tsukuda@tadakayo.jp`。**`--account` 必須**（一時ディレクトリは `login:use` 未設定＝279アカウントで `iam.serviceAccounts.ActAs` 403）
- **push手順**: origin は `npo-tadakayo/kjk-tadakayo`。gh のアクティブが `ytsukuda4470` だと403 → `gh auth switch -u tsuku-29` → push
- ⚠️ **タダカヨ側の認証情報がセッション中に消えることがある**（2026-08-12 に firebase CLI と gh の両方で発生）。gh は `tsuku-29` が候補から消えて `gh auth switch -u tsuku-29` が「no accounts matched」になり、push が ytsukuda4470 名義で403になった。**復旧は `gh auth login`（ブラウザ＝次田さん操作）**。firebase CLI も同様に279へ入れ替わる（→ Issue #3）。同じ根っこ（279作業と共有の認証ストアが上書きされる）と見られる
- **firebase MCP は使わない**（279 の tougou-db に接続）。kjk-tadakayo の Firestore は**タダカヨのADCトークンで REST 直叩き**（`CLOUDSDK_ACTIVE_CONFIG_NAME=tadakayo` → `gcloud auth print-access-token` → `firestore.googleapis.com/v1/projects/kjk-tadakayo/...`）
- **デプロイは rule05 二段階**: `hosting:channel:deploy {ch} --only admin` → curl検証 → `hosting:clone …:live`（live 昇格は自動承認でブロックされるため次田さんが実行）
- 帳票ページ（supply-print / report）は crm.css を読まない独立CSS。ブランド色を変える時は両方直す
- `/supply.html` は 301 で `/supply` にリダイレクト（curl 検証時は `-L` を付ける）
- **「公開されているか」の確認は curl の目視ではなく version マニフェストで行う**（2026-08-12 に偽陰性を出した）。`GET https://firebasehosting.googleapis.com/v1beta1/sites/{site}/versions/{versionId}/files?pageSize=300` が**そのリリースで実際に配信されていた全ファイル**を返す。curl はファイル名にコロン・日本語・特殊文字が入ると照会が壊れて404になり「公開されていない」と誤読する
- **hosting lp の public は `.`（リポジトリ直下）**。git 管理外のファイルもデプロイされるので、**ルート直下に内部資料を置いたら firebase.json の ignore に必ず追記する**。確認は `node -e "const{listFiles}=require('/opt/homebrew/lib/node_modules/firebase-tools/lib/listFiles.js');console.log(listFiles('.',JSON.parse(require('fs').readFileSync('firebase.json','utf8')).hosting[0].ignore).sort().join('\n'))"`（hosting エミュレータは ignore を無視するので検証に使えない）
- **ignore に日本語パスを書くときは濁点に注意**: Drive上のフォルダ名は NFD（濁点分解）なので、JSON に NFC で書いた `議事録など/**` はマッチしない。濁点を含まない前方一致（`議事*/**`）にする
- **firebase CLI の認証情報が279のものと入れ替わることがある**（2026-08-10 発生・同日復旧済み）: 症状は「`login:list` は `yoshinao-tsukuda@tadakayo.jp` を active と表示するのに、`projects:list` に出るのは tougou-db / voice-memo など**279のプロジェクト**で kjk-tadakayo が無い」→ `hosting:channel:deploy` が「Failed to get Firebase project」で落ちる。**復旧は `firebase login --reauth --account yoshinao-tsukuda@tadakayo.jp`（ブラウザ＝次田さん操作）**。復旧確認は `projects:list` に `kjk-tadakayo (current)` が出ること＋`hosting:channel:list --site kjk-tadakayo` が通ること
  - **回避策は 2026-08-13 にスクリプト化してリポジトリに常設した**（`scripts/deploy-hosting.mjs`・Issue #3 クローズ）。まず `node scripts/deploy-hosting.mjs --target lp --dry-run` で配信対象を確認し、`--channel {名前}` でプレビュー → 確認後に `--live`。以下は同スクリプトが内部でやっていることの説明:
  - gcloud のタダカヨ認証が生きていれば、**Hosting REST API で直接デプロイできる**。`gcloud auth print-access-token --account yoshinao-tsukuda@tadakayo.jp` ＋ ヘッダー `x-goog-user-project: kjk-tadakayo`（これが無いと quota project 未設定で403）。versions作成 → populateFiles → gzipしたファイルをhash単位でアップロード → FINALIZE → releases。⚠️ **firebase.json の headers/redirects は REST では形が違う**（`source`→`glob`、`[{key,value}]`→`{k:v}`、`type`→`statusCode`）。スクリプトは `/private/tmp/.../scratchpad/deploy-hosting.mjs`（セッション破棄で消えるので必要なら repo に移す）
- **タダカヨの gcloud ユーザー認証が期限切れ**（2026-08-08 時点）。Firestore REST 直叩きの前に `gcloud auth login`（tadakayo 設定）を通す。ADC は 279 アカウント（y.tsukuda@279279.net）のままなので kjk-tadakayo には使えない

## 再開コマンド

cd ~/Projects/tadakayo/01_システム開発/tadakayo-kiban && claude

## 関連URL・リソース

- LP本番: https://kjk.tadakayo.jp ／ CRM本番: https://kjk-tadakayo-admin.web.app
- GitHub: https://github.com/npo-tadakayo/kjk-tadakayo
- 仕様書: `ENGINEERING_NOTES.md`（正本）／ `MANUAL.md` ／ アプリ内 `/manual` `/engineering`
- アクセス解析: Clarity `wax7x03bg8` ／ GA4 `G-V70326L8MW`（タダカヨ側プロパティ 541485334）
- 旧セッション詳細: `HANDOFF_ARCHIVE.md`
